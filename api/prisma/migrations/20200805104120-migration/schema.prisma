datasource DS {
  provider = "sqlite"
  url = "***"
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

model Category {
    id          Int     @id @default(autoincrement())
    name        String
}
